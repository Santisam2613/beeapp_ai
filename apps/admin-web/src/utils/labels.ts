import type { NetworkVisibility } from '@/mocks/types';

export const VISIBILITY_LABELS: Record<NetworkVisibility, string> = {
  publico: 'Público',
  privado: 'Privado',
  solo_equipo: 'Solo equipo',
};
